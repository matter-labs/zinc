/*
    The Factorial.
*/

inputs {}

witness {
    result: field;
}

debug(0 as field);
let mut fact: field = 1;
debug(fact);
for i in 2..6 {
    fact = fact * i as field;
    debug(fact);
};

require(fact == result);
