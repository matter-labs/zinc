fn another(x: u8) -> u8 {
    let mut a = 5; // a1
    let b = if false { // cond1
        if false { // cond2
            a = 10; // a2
            2
        } else {
            a = 15; // a3
            3
        } // a4
    } else {
        a = 20; // a5
        4
    }; // a6
    a * b * x
}

fn main(input: u8) -> u8 {
    another(input)
}
