//!
//! The Curve Stableswap contract transactions.
//!

use crate::types::token_id::TokenId;
use crate::types::Address;
use crate::types::Balance;

///
/// This entity mirrors another one which is signed by Zargo.
///
pub struct Transaction {
    from: Address,
    to: Address,
    token_id: TokenId,
    amount: Balance,
}
