enum List {
    FIRST = 1,
    SECOND = 2,
    THIRD = 3,
}

impl List {
    pub fn first() -> Self {
        Self::FIRST
    }

    pub fn second() -> Self {
        Self::SECOND
    }

    pub fn third() -> Self {
        Self::THIRD
    }
}

fn main(witness: field) -> field {
    (List::first() + List::second() + List::third()) as field * witness
}
