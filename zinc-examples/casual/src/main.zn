contract Test {
    a: u8;
    b: u8;

    fn main() -> Self { Self { a: 5, b: 10 } }
}

#[test]
fn test_1() {
    let mut _contract = Test::main();
    _contract.a += 5;
    _contract.b += 10;

    assert!(_contract.a == 10);
    assert!(_contract.b == 20);
}

#[test]
#[should_panic]
fn test_2() {
    assert!(false, "panic message");
}

#[test]
fn test_3() {
    assert!(true);
}

#[test]
fn test_4() {
    assert!(true);
}
