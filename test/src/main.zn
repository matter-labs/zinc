struct Output {
    to_bits: [bool; 256],
    from_bits: field,
}

fn main() -> Output {
    let value: field = 42;
    let to_bits = std::to_bits(value);
    let from_bits = std::from_bits(to_bits);
    struct Output {
        to_bits: to_bits,
        from_bits: from_bits,
    }
}
