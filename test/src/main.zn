struct Output {
    sha256: [bool; 256],
    pedersen: (field, field),
}

fn main() -> Output {
    dbg!("Zello, World!");
    let preimage = [false, true, false, true];

    struct Output {
        sha256: std::sha256(preimage),
        pedersen: std::pedersen(preimage),
    }
}
